<UserControl x:Class="DesktopApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Sistema de Gestión Hotelera"
               Foreground="{StaticResource SecondaryBrush}"
               FontSize="28"
               FontWeight="SemiBold"
               TextAlignment="Center"
               HorizontalAlignment="Center"
                   Margin="0 20 0 0"/>

        <UniformGrid Columns="2" Rows="2" Grid.Row="1" Margin="30">

            <Border Style="{StaticResource CardBorder}" Height="190" Margin="0,0,20,20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding TotalRooms}" FontSize="54" FontWeight="SemiBold"
                               Foreground="#E8753D" TextAlignment="Center"/>
                    <TextBlock Text="Total de habitaciones" FontSize="16" TextAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}" Height="190" Margin="20,0,0,20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding TodayCheckIns}" FontSize="54" FontWeight="SemiBold"
                               Foreground="#E5A800" TextAlignment="Center"/>
                    <TextBlock Text="Check-ins de hoy" FontSize="16" TextAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}" Height="190" Margin="0,20,20,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding TodayCheckOuts}" FontSize="54" FontWeight="SemiBold"
                               Foreground="#3B82F6" TextAlignment="Center"/>
                    <TextBlock Text="Check-outs de hoy" FontSize="16" TextAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}" Height="190" Margin="20,20,0,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding ActiveReservations}" FontSize="54" FontWeight="SemiBold"
                               Foreground="#10B981" TextAlignment="Center"/>
                    <TextBlock Text="Reservas activas" FontSize="16" TextAlignment="Center"/>
                </StackPanel>
            </Border>

        </UniformGrid>
    </Grid>
</UserControl>
